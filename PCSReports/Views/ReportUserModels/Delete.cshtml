@model PCSReports.Models.ReportUserViewModel

@{
    ViewBag.Title = "Delete Report User";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.reportUser.username)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.reportUser.username)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.reportUser.ReportID)
        </dt>

        <dd>
            @Model.lsReports.FirstOrDefault(rt => rt.Value == Model.reportUser.ReportID.ToString()).Text.ToString()
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.reportUser.isActive)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.reportUser.isActive)
        </dd>
    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-primary" /> |
            @Html.ActionLink("Back to List", "Index")
        </div>
    }
</div>
