@model DateTime?

@Html.TextBox("", (Model.HasValue ? String.Format("{0:d}", Model.Value.ToShortDateString()) :
    string.Empty), new
    {
        @class = "datepicker form-control",
        @type = "date",
        @readonly = "readonly"
    })

